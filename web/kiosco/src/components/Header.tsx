export default function Header() {
  return (
    <header className="bg-verde-900 text-white px-[var(--pad)] py-5 flex items-center shrink-0 shadow-[0_2px_12px_rgba(20,58,42,0.15)]">
      <h1 className="font-display text-[length:var(--fs-section)] font-semibold tracking-wide">
        Hotel del Valle
      </h1>
    </header>
  )
}
